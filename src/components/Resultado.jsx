import React from 'react';

const Resultado = ({ objeto }) => {

  return <div>&nbsp;S({objeto.resultado.ter}) = 4 * {objeto.resultado.pri} - {objeto.resultado.tri} + {objeto.resultado.fib} = {objeto.resultado.op} </div>;
  

};

export default Resultado;